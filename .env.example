# Frontend environment variables (for local development only)
# Copy this file to .env — never commit .env to git

# URL of your Render backend (use localhost for local dev)
VITE_API_URL=http://localhost:3000
